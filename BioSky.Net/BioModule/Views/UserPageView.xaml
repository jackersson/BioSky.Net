<UserControl x:Class="BioModule.Views.UserPageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:BioModule.ViewModels"
             xmlns:local="clr-namespace:BioModule.Views"
             xmlns:cal="http://www.caliburnproject.org"   
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="400"
             d:DataContext="{d:DesignInstance vm:UserPageViewModel, IsDesignTimeCreatable=False}"
             >

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Controls.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="48"/>
            <ColumnDefinition Width="48"/>
            <ColumnDefinition Width="48"/>

        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="64"/>
            <RowDefinition Height="48"/>
            <RowDefinition Height="48"/>
            <RowDefinition Height="48"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="48"/>

        </Grid.RowDefinitions>
        
        <TabControl Grid.Column="1" Grid.RowSpan="5" HorizontalAlignment="Stretch" 
                    VerticalAlignment="Stretch" ItemsSource="{Binding Items}" Grid.ColumnSpan="4"                
                    SelectedItem="{Binding ActiveItem}"
                    cal:Message.Attach="[Event SelectionChanged]=[Action OpenTab]"
                    >
            <TabControl.Resources>
                <DataTemplate DataType="{x:Type vm:UserInformationViewModel}">
                    <local:UserInformationView x:Name="ActiveItem"></local:UserInformationView>
                </DataTemplate>
                <DataTemplate DataType="{x:Type vm:UserContactlessCardViewModel}">
                    <local:UserContactlessCardView x:Name="ActiveItem"></local:UserContactlessCardView>
                </DataTemplate>
            </TabControl.Resources> 
            <TabControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock
        				Text="{Binding DisplayName}" />
                </DataTemplate>
            </TabControl.ItemTemplate> 
            <TabControl.ContentTemplate>
                <DataTemplate>
                    <ContentControl Content="{Binding }" ></ContentControl>
                </DataTemplate>
            </TabControl.ContentTemplate>
        </TabControl>


        <Button  Grid.Row="5" Grid.Column="2" 
                 HorizontalAlignment="Stretch"
                 VerticalAlignment="Stretch"
                 cal:Message.Attach="[Event Click]=[Action Apply]"
                 Style="{DynamicResource MetroCircleButtonStyle}" >
            <Image Width="24" Height="24" Source="{Binding OkIconSource}" />
        </Button>

        <Button Grid.Row="5" Grid.Column="3" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"  Style="{DynamicResource MetroCircleButtonStyle}" >
            <Image Width="24" Height="24" Source="{Binding CancelIconSource}" />
        </Button>

        <Button Grid.Row="5" Grid.Column="4" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"  Style="{DynamicResource MetroCircleButtonStyle}" >
            <Image Width="24" Height="24" Source="{Binding DeleteIconSource}" />
        </Button>
       
        <local:ImageView  Content="{Binding CurrentImageView}" Grid.Column="0"
                          HorizontalAlignment="Stretch"  
                          VerticalAlignment="Stretch"                     
                          Grid.RowSpan="6">
            <local:ImageView.Resources>
                <DataTemplate DataType="{x:Type vm:ImageViewModel}">
                    <local:ImageView Content="CurrentImageView" DataContext="{Binding Mode=OneWay}"/>
                </DataTemplate>
            </local:ImageView.Resources>
        </local:ImageView>

    </Grid>
</UserControl>
