<UserControl x:Class="BioModule.Views.TrackControlView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"                     
             xmlns:cal="http://www.caliburnproject.org"
             xmlns:vm="clr-namespace:BioModule.ViewModels"              
             xmlns:local="clr-namespace:BioModule.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <UserControl.DataContext>
        <vm:TrackControlViewModel/>
    </UserControl.DataContext>

    <UserControl.Resources>
        <DataTemplate DataType="{x:Type vm:TrackControlItemViewModel}">
            <local:TrackControlItemView></local:TrackControlItemView>
        </DataTemplate>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition Height="53*"/>

        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="48"/>
            <ColumnDefinition Width="48"/>
            <ColumnDefinition Width="48"/>

        </Grid.ColumnDefinitions>

        <Button  Style="{DynamicResource MetroCircleButtonStyle}" x:Name="button" Grid.Column="1" Grid.Row="0" HorizontalAlignment="Left" VerticalAlignment="Top" Width="48" Height="48">
            <Image Source="{Binding AddIconSource}" Width="32" Height="32" />
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="Click">
                    <cal:ActionMessage MethodName="AddMenu" />
                </i:EventTrigger>
            </i:Interaction.Triggers>
        </Button>

        <Button Style="{DynamicResource MetroCircleButtonStyle}" x:Name="button_Copy" Grid.Row="0" HorizontalAlignment="Left" VerticalAlignment="Top" Width="48" Height="48" Grid.Column="2">
            <Image Source="{Binding RemoveIconSource}" Width="32" Height="32"  />
        </Button>

        <Button Style="{DynamicResource MetroCircleButtonStyle}" x:Name="button_Copy1" Grid.Column="4" Grid.Row="0" HorizontalAlignment="Left" VerticalAlignment="Top" Width="48" Height="48">
            <Image Source="{Binding DeleteIconSource}" Width="32" Height="32"  />
        </Button>


        <ListBox  Grid.Row="0" BorderThickness="2" ItemsSource="{Binding TrackControlItems}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Caption}"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        
        <ContentControl x:Name="CurrentViewTab" Grid.Row="1" Grid.ColumnSpan="4">
            <ContentControl.ContentTemplate>
                <DataTemplate>
                    <local:TrackControlItemView Content="{Binding ScreenViewModel}"></local:TrackControlItemView>
                </DataTemplate>
            </ContentControl.ContentTemplate>
        </ContentControl>
        
    </Grid>
</UserControl>
